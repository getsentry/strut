```
$ python -m strut --help
Usage: __main__.py [OPTIONS]

Options:
  --cache PATH           path to cache song downloads
  --project TEXT         Google project id
  --subscription TEXT    pubsub subscription name
  --max-latency INTEGER  max latency in seconds before message is discarded
  --help                 Show this message and exit.
```
